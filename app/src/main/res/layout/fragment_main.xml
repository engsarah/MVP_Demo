<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center|top"
        android:layout_marginVertical="100dp"
        android:textSize="20dp"
        android:textStyle="bold"
        android:textColor="@color/colorAccent"
        android:text="@string/title_new_features"/>

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/btn_display_Menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title"
        android:background="@drawable/round_corners_accent"
        android:layout_marginLeft="@dimen/Image_view_margin"
        android:layout_marginRight="@dimen/Image_view_margin"
        android:layout_marginBottom="@dimen/layout_margin"
        android:textColor="@color/white"
        android:textSize="15dp"
        android:text="@string/display_hint"
        android:textStyle="italic" />

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/btn_image_loader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_display_Menu"
        android:layout_centerInParent="true"
        android:background="@drawable/round_corners_accent"
        android:layout_margin="20dp"
        android:textColor="@color/white"
        android:textSize="15dp"
        android:text="@string/load_image_btn"
        android:textStyle="italic" />

</RelativeLayout>